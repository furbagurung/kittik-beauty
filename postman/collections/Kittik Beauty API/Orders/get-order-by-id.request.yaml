$kind: http-request
method: GET
url: '{{baseUrl}}/orders/:id'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '1'
    description: 'Numeric order id belonging to the authenticated user'
